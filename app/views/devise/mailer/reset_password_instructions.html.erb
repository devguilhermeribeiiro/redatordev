<div class="flex items-center justify-center min-h-screen bg-gray-100">
  <div class="max-w-md w-full bg-white p-8 rounded-lg shadow-lg text-center">
    <p class="text-lg font-medium text-gray-800 mb-4">Hello <%= @resource.email %>!</p>

    <p class="text-gray-600 mb-4">Someone has requested a link to change your password. You can do this through the link below.</p>

    <p class="mb-4">
      <%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token), class: "text-blue-500 hover:underline" %>
    </p>

    <p class="text-gray-600 mb-4">If you didn't request this, please ignore this email.</p>

    <p class="text-gray-600">Your password won't change until you access the link above and create a new one.</p>
  </div>
</div>

